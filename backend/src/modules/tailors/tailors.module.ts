import { Module } from '@nestjs/common';

// TailorsModule owns everything under /tailors. Empty by design at this phase —
// see docs/ROADMAP.md for when this module's endpoints land.
@Module({})
export class TailorsModule {}
