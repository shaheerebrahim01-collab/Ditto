import { Module } from '@nestjs/common';

// RentalsModule owns everything under /rentals. Empty by design at this phase —
// see docs/ROADMAP.md for when this module's endpoints land.
@Module({})
export class RentalsModule {}
