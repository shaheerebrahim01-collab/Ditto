# Shell scripts and Docker-related files run inside Linux containers —
# force LF regardless of the checkout platform's core.autocrlf, or a
# Windows checkout breaks `#!/bin/sh` with a CRLF-corrupted shebang.
*.sh text eol=lf
Dockerfile text eol=lf
Caddyfile text eol=lf
